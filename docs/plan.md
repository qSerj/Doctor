# План

Порядок собран из соотношения «цена — подтверждённость». Статус: **Предложение**, кроме Э0, который сделан.

## Э0. Каркас — сделано

Solution, четыре проекта и тесты, общие свойства сборки, двор для пациентов, документация. Ядро опознаёт файл шоу, CLI читает его в правильной кодировке, тесты зелёные.

Критерий: `dotnet test` проходит, CLI на боевом файле отвечает «файл шоу опознан».

## Э1. Разбор и инвентарь

Полный разбор файла шоу: слайды, слои, ключевые кадры, медиа, звук. Инвентарь: типы, форматы, разрешения, частоты кадров, длительности. Главное число — сумма распакованных пикселей.

Целиком без Windows.

Критерий готовности честный и проверяемый: на боевом проекте разбор выдаёт 29 слайдов, 351 слой, 444 с учётом слоёв внутри переходов, 414 заменяемых мест шаблона и 1.74 ГБ распакованными.

## Э2. Правила приёмки и отчёт

Арифметические правила из [диагностики](diagnostics.md) и отчёт в CLI. На этом этапе доктор **ничего не пишет**, значит ничего не может испортить.

Критерий: на боевом проекте отчёт называет негабаритные исходники, чужой корень, разнобой частот кадров и музыку длиннее фильма.

## Э3. Первая поездка на Windows

Гигиена окружения: найти кэш, его размер и отказы записи, копию в `VirtualStore`, лишние процессы. Одновременно — проверка гипотезы механизма. Одна поездка, два результата.

Критерий: на живой машине доктор объясняет состояние установки и умеет переименовать кэш с возможностью вернуть.

## Э4. Лечения

Сначала замер съедобности форматов синтетическими файлами шоу — без него лечение может лечить не то. Затем два точных лечения и запись нового файла шоу рядом со старым.

Гейт: проверить, переживает ли программа правку файла шоу со стороны.

## Э5. Наблюдатель

Дешёвый уровень: счётчики ввода-вывода, процессорное время, рабочее множество, признак незанятой очереди сообщений, рост выходного файла у процесса кодирования. Плюс журнал, который заодно собирает разметку для порогов.

Гейт: выбрать способ узнать, какой файл процесс читает прямо сейчас.

## Э6. Интерфейс

Трей, уведомление, окно отчёта с одной кнопкой. Поведение описано в [отдельном документе](../design/behaviour.md) и должно быть согласовано до начала этапа.

## Что в план не входит

Средний и тяжёлый уровни наблюдения — трассировка событий ядра и разбор дампов. Появятся, только если дешёвого уровня не хватит.
